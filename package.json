{
  "name": "football-match-scrapper",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack React + Express app to scrape and display football matches",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,WORKER,CLIENT -c blue,magenta,green \"npm run dev --prefix server\" \"npm run worker:dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:worker": "npm run worker:dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "@vercel/functions": "^1.6.0",
    "axios": "^1.7.9",
    "cheerio": "^1.0.0"
  }
}
